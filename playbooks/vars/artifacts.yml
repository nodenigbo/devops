# it-migration job service Deploy

backup_path: '/opt/iTransRevamp/{{ instance }}/backup'
deployment_path: '/opt/iTransRevamp/{{ instance }}'
source_path: '/var/lib/jenkins/workspace/{{ jenkins_job }}/migration-job/target'
prefix: ' '
service_name: '{{ instance }}'
